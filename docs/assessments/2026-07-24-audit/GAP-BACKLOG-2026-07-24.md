# Gap backlog (P-AUDIT, 2026-07-24)

Ordered BLOCKER → MAJOR → MINOR; dependency-first within each tier (a foundation item before a module item that depends on it). "Owning module" of `cross-cutting` means no single `src/<module>` change resolves it. "Evidence of done" is what a future verifier should check — this audit did not implement any of it.

---

## BLOCKER

| id | Description | Traces to | Owning module | Status | Handbook prompt | Evidence of done |
|---|---|---|---|---|---|---|
| G-007 | ~65 modified/added/deleted files sit uncommitted beyond the last commit (`1062400`, "P-16"), including the entire cross-cutting E2E/degradation/idempotency/authorization-matrix suite, new migrations for 7 modules, and identity/catalog/billing/search infra changes. None of this is in git history. | Process/traceability (Playbook Sec 16 "AI operates inside frozen contracts", general engineering hygiene) | cross-cutting | MISSING (from git history) | new work | `git status --porcelain` is empty (or every remaining entry is deliberately excluded, e.g. `.env`/scratch files) on the branch this work is meant to ship on. |
| G-001 | QG-01 (lint) fails: 68 files fail `ruff format --check`; `ruff check` reports 50 errors (18 auto-fixable, 32 more with `--unsafe-fixes`). | Playbook Sec 16 QG-01 | cross-cutting (identity, moderation, notifications, tests) | BROKEN | new work (hygiene pass) | `bash scripts/lint.sh` exits 0. |
| G-002 | QG-02 (mypy --strict) fails: 31 errors, all missing type annotations on pytest fixtures in `apps/backend/tests/backbone/`. Zero errors in any `src/` file. | Playbook Sec 16 QG-02 | backbone (tests only) | BROKEN | new work (hygiene pass) | `bash scripts/typecheck.sh` exits 0. |
| G-003 | QG-03 (pytest) fails: 5 failed, 3 errors out of 1,715 items attempted. See G-008 for the itemized breakdown. | Playbook Sec 16 QG-03 | catalog, media, performance-harness | BROKEN | new work (triage — see G-008) | `bash scripts/test.sh` exits 0. |
| G-004 | QG-06 (contract drift) fails: 15 routes flagged. ~13 are a cosmetic camelCase/snake_case path-parameter mismatch in `configuration`'s already-shipped routers vs. `contracts/openapi.yaml` (functionally identical routes; FastAPI doesn't match on param names). The remaining 3 (`Business Profiles` team-management ops) are genuinely unimplemented — see G-011. | DEC-21/AIR-19, QG-06 | configuration | BROKEN (mostly cosmetic) | new work (mechanical rename in `configuration/interfaces/routers.py` only) | `python3 tools/check_contract_drift.py contracts/openapi.yaml` exits 0. |
| G-005 | QG-07 (security) fails: bandit finds 8 issues (1 medium: possible-SQL-injection-shaped f-string in `backbone/persistence/schema_role.py:55`, 7 low: `assert` in source). `pip-audit` finds 20 known CVEs in `pillow==12.2.0` (fix at `12.3.0`), used directly in the media-intake image pipeline. `gitleaks` UNVERIFIED locally (not installed; CI-only). | Security Architecture doc, QG-07 | backbone, notifications, media (dependency) | BROKEN | new work | `bash scripts/security-scan.sh` exits 0 (with gitleaks installed/run in CI). |
| G-006 | QG-10 (frontend) will fail on the next CI run: its guard clause no-ops only when `apps/frontend/package.json` is absent; that file now exists (frontend scaffolded this session), so the guard falls through to `exit 1` pending real build/lint/test steps. | Playbook Sec 16 QG-10, CLAUDE.md "apps/frontend not scaffolded yet" (now stale) | frontend / CI | BROKEN (imminent) | new work | `.github/workflows/ci.yml`'s `qg10-frontend` job runs real build/lint/test steps (not the placeholder guard) and passes. |

---

## MAJOR

| id | Description | Traces to | Owning module | Status | Handbook prompt | Evidence of done |
|---|---|---|---|---|---|---|
| G-008 | 5 real pytest failures + 3 errors, itemized: (a) `catalog::test_concurrent_save_raises_stale_data_error` — optimistic lock did not raise `StaleDataError`; (b) `media::test_storage_key_uniqueness_is_enforced` — FAILED + setup ERROR, unique-constraint collision on a repeated fixture `storage_key`, likely test-isolation/ordering; (c) `media::test_run_once_advances_an_asset_through_scan_and_processing` FAILED; (d) `performance::test_search_benchmark` FAILED, "every request errored"; (e) `performance::test_write_path_benchmark` FAILED, `CategoryNotFoundError` — depends on category fixture data not present/consistent in full-suite run order; (f) `performance::test_benchmark_interactive` ERRORed, `RuntimeError: Event loop is closed`. Reproduced identically on two independent runs. | QG-03 | catalog, media, performance-harness | BROKEN | new work (triage; likely test-isolation/ordering bugs, not domain-logic bugs, but not confirmed) | All 5+3 pass/error clear on two consecutive full-suite runs. |
| G-009 | Pre-existing, repo-wide `MissingGreenlet` bug in every module's `Sqlalchemy<Aggregate>Repository.save()`: flushing then immediately reading a `server_default`/`onupdate` column off the just-flushed row triggers an implicit expired-attribute refresh outside the SQLAlchemy asyncio greenlet context. Self-documented identically in `ads/README.md`, `notifications/README.md`, `moderation/README.md` "Known gaps"/run-notes. | Correctness (async ORM usage) | cross-cutting (every module's repository base) | BROKEN (latent, not currently failing tests but documented as reproducible) | new work | The pattern is fixed once in `backbone`'s repository base (or the shared save-then-read idiom) and re-verified across at least one affected module. |
| G-010 | Several event-projection handlers are fully built and unit/integration-tested but never wired to a live `OutboxDispatcher` in `composition_root.py`: media's outbox has no dispatcher draining it at all (affects `catalog`'s, `profiles`'s, and `ads`'s own media-status consumers identically); `analytics`'s `ListingViewed`/`ContactButtonClicked`/`PremiumListingStat` consumers have no real producer (catalog never calls `outbox.append(...)` for them). | FR-ADV-*, I-22/I-23 downstream completeness | catalog, profiles, ads, analytics, media (cross-cutting wiring decision) | PARTIAL | new work (needs a multi-consumer fan-out design decision first, per every module's own README caveat — not safely done piecemeal) | Media's outbox has exactly one dispatcher draining it in `composition_root.py`, proven by an integration test exercising a real `MediaAssetRejected` → downstream effect; catalog emits the three named events for real. |
| G-011 | 3 `Business Profiles` OpenAPI operations (`addTeamMember`/`listTeamMembers`/`removeTeamMember`) are frozen in `contracts/openapi.yaml` but back no approved Domain Model concept (`BusinessProfile` lists only `PortfolioItem` as a child entity). `profiles/README.md` documents this explicitly and declines to invent the concept. | AIR-19, DDD Sec 5.2 | profiles | MISSING (deliberately, pending decision) | new work — needs a human ADR first (implement as a new aggregate/cross-module port, or remove from the frozen contract) | Either 3 new passing routes exist, or an ADR formally removes the 3 operations from `contracts/openapi.yaml`. |
| G-012 | Identity: (a) no `confirmEmail` operation exists despite the Security Architecture doc and `registerEmail`'s own description implying one — accounts are `ACTIVE` immediately, `verified_at` can never be set; (b) email-based account recovery has no completion endpoint (phone-based recovery works); (c) `Idempotency-Key` header is accepted per contract but never deduped anywhere. | Security Architecture (confirmation-link control), FR-AUTH-002/006 | identity | PARTIAL | new work — needs a human ADR for (a)/(b) (add operations or formally descope for v1); (c) is a smaller, no-ADR-needed fix | A passing test exercises the full confirm-email round trip (or an ADR formally descopes it); same for recovery; a duplicate request with the same `Idempotency-Key` returns the cached result rather than re-executing. |
| G-013 | Analytics: `PHONE_REVEALED` never updates `ListingStatistics` (needs `messaging` to add `listingId` to that event's payload — a cross-module change); `USER_GROWTH` report has no data source in v1 (`UserRegistered` isn't in the closed I-22/metric scope) and honestly returns `{"available": false}` rather than a fabricated count. | FR-ANALYTICS-002, FR-ADMIN-005 | analytics + messaging (payload change) | PARTIAL (self-documented, honest placeholder) | new work — needs an ADR if `USER_GROWTH` is to be closed (extends I-22's scope or adds a dedicated ingestion path) | `ListingStatistics.phoneReveals` reflects real data; `USER_GROWTH` either has a ratified data source or is formally descoped. |
| G-014 | `catalog`'s `getListingStatistics` hardcodes `null` for `views`/`contactClicks`/`phoneReveals`/`chatsInitiated` — the real data lives in `analytics`, which nothing may import, and `catalog`'s router has no injection point for it. Self-documented, explicitly attributed to analytics in `catalog/README.md`. | FR-ANALYTICS-002 | catalog + analytics (cross-module wiring decision) | PARTIAL (honest placeholder, not silent) | new work — needs an architecture decision on how a leaf-importing module surfaces sink-module data (a read port? a projection into catalog's own schema?) | `getListingStatistics` returns real, non-null values for all four fields. |
| G-015 | 11 of 21 NFRs are explicit GAPs: `NFR-PERF-001/002` (p95 SLOs unmeasured), `NFR-SCALE-001/002` (load/capacity unproven), `NFR-AVAIL-001` (availability target), `NFR-REL-001` (RPO/RTO), `NFR-SEC-002` (encryption at rest/in transit — TLS explicitly TODO in `deployment/nginx/nginx.conf`), `NFR-SEC-005` (anti-scraping/bot mitigation), `NFR-ACC-001` (WCAG — no frontend audit done, and a frontend now exists as of this session), `NFR-BAK-001` (backup/restore), `NFR-REC-001` (DR recovery). All explicitly out of this task's stated scope per `docs/assessments/2026-07-24-acceptance/gap_report.md`'s own accounting (P-21 performance, P-22 security hardening). | SRS NFR section | cross-cutting / infra | GAP (explicitly scoped out, not overlooked) | P-21 (performance tuning), P-22 (security hardening) | Each NFR has a real measurement or test asserting the documented threshold. |
| G-016 | No Dockerfile or compose service exists for the main API application (`main.py`'s FastAPI app) — only the separate realtime WebSocket gateway is containerized (`deployment/docker/realtime.Dockerfile`). `docker-compose.yml` has no `api` service. | Infrastructure and Deployment Architecture doc | deployment | MISSING | new work (likely folded into P-22 or a dedicated infra task) | `docker compose up` brings up a working `api` container serving `/health`/`/ready`. |
| G-017 | No CD pipeline exists (`.github/workflows/` has only `ci.yml`); no backup/restore automation or tested-restore evidence found anywhere in the repo; no staging deployment. `AC-DEPLOY` is explicitly unverifiable per the acceptance mapping and this audit's independent check. | Infrastructure and Deployment Architecture doc, DevSecOps Architecture doc | deployment | MISSING | P-22 or a dedicated deployment task | A `cd.yml` exists and has run at least once; a documented backup has been restored at least once, with evidence. |
| G-018 | `NFR-REL-002`'s third named degradation clause ("chat unavailability SHALL degrade to click-to-call") has no covering test — the other two clauses (search→Postgres fallback, media-lag-doesn't-block-publish) do. | NFR-REL-002 | messaging + cross-cutting | MISSING | new work | `tests/degradation/test_messaging_chat_down_falls_back_to_click_to_call.py` (or equivalent) exists and passes against a real simulated realtime-gateway failure. |
| G-019 | `configuration` is the only one of 13 modules without a `TRACEABILITY.md`, and is the one module with real, uncorrected contract drift (the camelCase/snake_case path-parameter mismatch underlying G-004) — a prior task explicitly left it untouched under AIR-01 ("not this task's module to fix"). | Playbook Sec 5 (traceability convention) | configuration | PARTIAL | new work (small, well-specified — see G-004) | `configuration/TRACEABILITY.md` exists with the same FR/BRULE/I-nn → code/test mapping every other module has; path parameters match the contract exactly. |
| G-020 | `apps/frontend` was scaffolded (this session) on React 19 + TanStack Start, deviating from the Frontend Architecture & Engineering Handbook's specified Next.js. Everything else (Tailwind v4, shadcn/ui, uz/ru/en i18n) matches. Flagged in `apps/frontend/README.md` as an open question, not yet ratified. | Frontend Architecture & Engineering Handbook, AIR-19 | frontend | PARTIAL (deviation, undecided) | new work — needs a human ADR (ratify TanStack Start, or replace with Next.js) | An ADR exists either ratifying the current stack or directing a migration, and the handbook/README are reconciled with it. |

---

## MINOR

| id | Description | Traces to | Owning module | Status | Handbook prompt | Evidence of done |
|---|---|---|---|---|---|---|
| G-021 | Three stray git worktrees under `.claude/worktrees/` (`billing-p09`, `identity-p05`, `p04-configuration`), ~1GB total, left unpruned after their branches were presumably merged. | Repo hygiene | cross-cutting | n/a (process debt) | new work | `git worktree list` shows only the main worktree; the branches are deleted if fully merged. |
| G-022 | The `test_I<nn>_*`/`test_FR_*`/`test_NFR_*` naming convention (Playbook Sec 5, CLAUDE.md's own naming table) is followed inconsistently: only 2 of 89 FRs are literally name-traced; the `test_I<nn>_` pattern is independently reused for at least 4 unrelated local numbering schemes across the codebase (see `INVARIANT-COVERAGE-2026-07-24.md`'s caveat). Real coverage exists in nearly every case — this is a traceability-tooling risk, not a functional defect. | Playbook Sec 5 | cross-cutting | PARTIAL (real coverage, unreliable naming) | new work (apply the convention going forward for new tests; consider a dedicated retrofit task only if literal FR-id grep-traceability becomes a hard compliance requirement) | A grep for `test_I<the global id>_` in the module the Domain Model assigns it to reliably finds a real, correct test, for all 24. |
| G-023 | `apps/frontend/src/locales/` has only `uz.json`, not split into `uz_latn`/`uz_cyrl` per DEC-19. | DEC-19 | frontend | MISSING | new work | Two locale files exist and cross-script search/UI both exercise both. |
| G-024 | `admin`'s `OperatorSessionContext` has no OpenAPI operation yet — built ahead of the frontend session that will call it, per the module's own stated charter. Deliberate, not urgent. | admin's own charter | admin | MISSING (deliberate) | new work, low priority | An operation exists once the frontend needs it. |
| G-025 | Notifications: recipient locale always resolves to `uz_latn` (no per-user locale field persisted anywhere yet); web-push has a real port/adapter but no subscription-registration surface exists in any approved document; the `ads` event consumer is unverified against real payloads since `ads` was, until recently, still growing out its own implementation. | FR-NOTIF-*, DEC-19 | notifications + identity (locale field) | PARTIAL | new work | A real per-user locale field exists and is read by dispatch; a subscription-registration operation exists if/when approved. |
| G-026 | Moderation: `CONVERSATION`-subject cases can never reach a messaging-specific command — no approved document names one beyond "suspend the reported user's account". | DDD moderation command set | moderation | MISSING (deliberately, no document to build against) | new work — needs a human decision first | A document names a conversation-directed command, and moderation implements it. |
| G-027 | Identity's own README explicitly flags `Idempotency-Key` as accepted-but-not-deduped for `verifyOtp`/`registerEmail` (also listed under G-012 for completeness — kept as its own line since it's independently actionable and lower-stakes than the confirmation-flow gaps). | API contract cross-cutting rule (`Idempotency-Key` header) | identity | MISSING | new work, small | A repeated request with the same `Idempotency-Key` returns the original result rather than re-executing. |

---

## Backlog summary

- **BLOCKER: 7** (G-001…G-007)
- **MAJOR: 13** (G-008…G-020)
- **MINOR: 7** (G-021…G-027)
- **Total: 27 tracked items**

Cross-reference: the full narrative for every item above, including the evidence this audit gathered (test names, file paths, direct command output), is in `AUDIT-REPORT-2026-07-24.md`.
