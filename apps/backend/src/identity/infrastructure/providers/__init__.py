"""identity/infrastructure/providers -- Eskiz SMS, Google OAuth, and SMTP email adapters. Every
provider SDK/response type is confined to its own module here (`provider-sdk-confined-to-infrastructure`,
tools/importlinter.cfg); only the plain-typed ports they implement cross back into `application/`.
"""
