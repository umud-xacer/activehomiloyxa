"""backbone.di -- see `example.py`'s module docstring: a wiring-pattern proof, not a real
feature. Nothing here is re-exported at the `backbone` top level on purpose, so it can't be
imported by accident where a real port/adapter/use-case is expected.
"""
